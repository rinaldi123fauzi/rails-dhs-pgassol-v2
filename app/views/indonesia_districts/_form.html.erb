<%= form_for(indonesia_district) do |f| %>
  <% if indonesia_district.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(indonesia_district.errors.count, "error") %> prohibited this indonesia_district from being saved:</h2>

      <ul>
      <% indonesia_district.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :city_id %>
    <%= f.number_field :city_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
